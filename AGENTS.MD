# Agent Instructions

- Before making any edit to an existing file, create a sibling backup named with the same path plus `.BAK` first.
- Keep the `.BAK` file in sync with the pre-edit state for that file before applying the change.
